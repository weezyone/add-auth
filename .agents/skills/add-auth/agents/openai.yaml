interface:
  display_name: "Add Auth"
  short_description: "Repo-specific patterns and workflows for add-auth"
  default_prompt: "Use the add-auth repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true